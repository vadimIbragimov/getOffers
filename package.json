{
  "name": "get-offers",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "lerna-clean": "lerna clean -y",
    "lerna-link": "lerna link",
    "lerna-bootstrap": "lerna bootstrap",
    "build:client": "lerna run build --scope=client-*",
    "build:packages-watch": "lerna run build:watch --scope=client-*",
    "start": "lerna run start",
    "build-parser-instance": "lerna run build --scope=@go/server-parsers-controller && lerna run build --scope=@go/server-parser-instance",
    "start-parser-instance": "lerna run start --scope=@go/server-parser-instance",
    "deploy server-parser-controller": "lerna run build --scope=@go/server-parsers-controller && lerna run start --scope=@go/server-parsers-controller"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
